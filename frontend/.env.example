# Local dev: safe to leave unset — lib/api.ts falls back to
# http://localhost:4000/api automatically.
#
# Production (Vercel): set this to your deployed backend's URL, e.g.
# https://your-backend.onrender.com/api
VITE_API_URL=http://localhost:4000/api
